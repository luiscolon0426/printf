#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>
#include <limits.h>

int _printf(const char *format, ...);
int printchar(char *s, ...);
int printdecimal(char *s, ...);
int printstr(char *s, ...);
int printint(char *s, ...);




#endif /* MAIN_H */
